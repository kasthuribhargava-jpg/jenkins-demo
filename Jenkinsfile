pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh 'echo Building from GitHub'
            }
        }

        stage('Test') {
            steps {
                sh 'echo Testing from GitHub'
            }
        }
    }
}